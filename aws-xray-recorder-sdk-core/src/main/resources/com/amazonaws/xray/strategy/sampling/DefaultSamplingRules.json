{
  "version": 1,
  "default": {
    "fixed_target": 1,
    "rate": 0.05
  },
  "rules": [
    // Example path-based rule below. Rules are evaluated in array order, the default rule will be used if none match the incoming request.
    //{
    //  "description": "Rule for the checkout page",
    //  "service_name": "*",
    //  "http_method": "*",
    //  "url_path": "/checkout",
    //  "fixed_target": 10,
    //  "rate": 0.05
    //},
    // A default rule, as included above, is required in any sampling rules file.
    // (service_name, http_method, and url_path are fixed to "*" for this rule).
  ]
}
